# QuantumForge Backend Environment Configuration
# Copy this to .env and configure with your credentials
# NEVER commit .env to git - add to .gitignore

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this-in-production-use-at-least-32-characters

# Database (if using PostgreSQL/MySQL instead of SQLite)
# DATABASE_URL=postgresql://user:password@localhost:5432/quantumforge

# IBM Quantum
# Get your token from: https://quantum-computing.ibm.com/
IBMQ_TOKEN=your-ibm-quantum-token-here

# AWS Braket (if using AWS quantum hardware)
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_REGION=us-east-1

# Flask Configuration
FLASK_ENV=development
FLASK_DEBUG=False

# CORS Origins (comma-separated)
# CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# API Rate Limiting
# RATE_LIMIT_PER_MINUTE=60
